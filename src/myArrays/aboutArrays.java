package myArrays;

public class aboutArrays {
	
	// An array is a collection of a singular data type, whose structure is indexed and contiguous in memory.
	
	/*
	 * Declare: creates a reference to the array
	 * Instantiation of array: creates the array
	 * Initialization: assign values to cells in an array.
	 * 
	 * Common operations of arrays:
	 * - inserting a value
	 * - traversing a given array
	 * - accessing given cell#
	 * - searching a given value
	 * - deleting a given value
	 */

}
